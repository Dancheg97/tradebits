
type User {
    id: ID!
    pubkey: String! @id @search(by: [hash])
    balance: Int!
    messages: [String]
    orders: [Order] @hasInverse(field: "user")
    operations: [Operation] @hasInverse(field: "user")
}

type Market {
    id: ID!
    name: String!
    pubkey: String!
    descr: String!
    img: String!
    worktime: String!
    fee: String!
}

type Order {
    id: ID!
    offer: Int!
    recieve: Int!
    ratio: Float!
    user: [User] @hasInverse(field: "orders")
    market: String!
}

type NetMember {
    id: ID!
    string: String! @id @search(by: [term])
    pubkey: String!
    link: String!
}

type Operation {
    id: ID!
    desciption: String!
    user: [User] @hasInverse(field: "operations")
}
